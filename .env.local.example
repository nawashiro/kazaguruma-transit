# サーバー専用の環境変数
GOOGLE_MAPS_API_KEY=your_api_key_here

# Cloudflare Tunnelの設定（本番運用時のみ）
CLOUDFLARE_TUNNEL_TOKEN=your_api_key_here

# 初回は `cp app-config.json.example app-config.json` で公開設定を生成する。
# app-config.json は配布先ごとの設定であり、Gitでは管理しない。
