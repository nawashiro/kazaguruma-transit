// The management route and the discussion-specific route intentionally share
// the same implementation so moderator behavior cannot drift between them.
export { default } from "../[naddr]/moderators/page";
